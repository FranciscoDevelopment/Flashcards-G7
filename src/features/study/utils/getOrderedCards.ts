import type { Card } from '../../cards/types';

// Punto de integración para D4:
// acá se implementará la lógica de priorización
// de tarjetas para los modos de estudio.

export function getOrderedCards(cards: Card[]): Card[] {
  return cards;
}